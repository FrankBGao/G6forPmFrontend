import React from 'react';
// import FormCustomizedFormControls from './ModelAttributersForm';
import Model from './ModelGraph/index';
// This module is for the graph visualization
// We use the G2 as the visualization package
// It allow the graph, interact with the node, user could click and drag nodes;
// user could see the graph and move it from the mini-map

export default Model;